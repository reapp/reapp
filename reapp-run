#!/usr/bin/env node

var Program = require('commander');

Program
  .option('-p, --production', 'run in production mode')
  .parse(process.argv);

var Server = require('reapp-server');

Server(process.cwd() + '/app/routes');

